batch
@echo off
setlocal

:menu
cls
echo 1. Hexxitt
echo 2. Vanilla
echo 3. PombinhasReforged
echo 4. Exit

set /p choice=Enter your choice: 

if "%choice%"=="1" (   
    powershell -Command git checkout main 
) else if "%choice%"=="2" (
    powershell -Command git checkout Vanilla 
) else if "%choice%"=="3" (
    powershell -Command git checkout MC-1.19.4-Modded
) else if "%choice%"=="4" (
    exit
) else (
    echo Invalid choice. Please try again.
    pause
    goto menu
)

java -Xmx3G -Xms1G -jar server.jar nogui

echo Add
powershell -Command "git add ."
echo Commit
powershell -Command "git commit -m 'Saved world'"
echo Push
powershell -Command "git push"
PAUSE
